import styled from "styled-components";

export const StyledFooter = styled.footer`
  height: 20vh;
  display: grid;
  place-items: center;

  .footer-title {
    font-size: var(--text-md);
    display: flex;
    align-items: center;
    gap: 2.2rem;
  }
`;
